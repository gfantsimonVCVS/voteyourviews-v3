voteyourviews-*.json

# Google API creds for photo proxy (never commit)
tools/photo-proxy.config.json

# OS + editor junk
.DS_Store
.claude/

# Photo export artifacts
images/travis-photos.zip

# Local Netlify folder
.netlify

# Local design-tool snapshots + safety backups (never commit)
.impeccable/
Backups/
